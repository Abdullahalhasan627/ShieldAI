<UserControl x:Class="ShieldAI.UI.Views.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- العنوان والأزرار -->
        <StackPanel Grid.Row="0">
            <Grid Margin="0,0,0,24">
                <TextBlock Text="العمليات الجارية" Style="{StaticResource HeaderStyle}"/>
                <Button Content="🔄 تحديث" 
                        Style="{StaticResource ModernButtonStyle}"
                        Command="{Binding ScanProcessesCommand}"
                        HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>
        
        <!-- قائمة العمليات -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <ListView ItemsSource="{Binding Processes}"
                      Style="{StaticResource ModernListViewStyle}"
                      ItemContainerStyle="{StaticResource ModernListViewItemStyle}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="الحالة" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock FontSize="16">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="✅"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSuspicious}" Value="True">
                                                        <Setter Property="Text" Value="⚠️"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsTrusted}" Value="True">
                                                        <Setter Property="Text" Value="🔒"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="PID" Width="70" 
                                        DisplayMemberBinding="{Binding ProcessId}"/>
                        
                        <GridViewColumn Header="الاسم" Width="150" 
                                        DisplayMemberBinding="{Binding ProcessName}"/>
                        
                        <GridViewColumn Header="المسار" Width="350" 
                                        DisplayMemberBinding="{Binding ExecutablePath}"/>
                        
                        <GridViewColumn Header="الذاكرة" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding MemoryUsage, Converter={StaticResource FileSizeConverter}}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="إجراء" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="إنهاء" 
                                            Padding="12,6"
                                            Command="{Binding DataContext.TerminateProcessCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource DangerButtonStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsTrusted}" Value="True">
                                                        <Setter Property="IsEnabled" Value="False"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
