<UserControl x:Class="ShieldAI.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="الإعدادات" Style="{StaticResource HeaderStyle}" Margin="0,0,0,24"/>
            
            <!-- إعدادات الحماية -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="الحماية في الوقت الحقيقي" Style="{StaticResource SubHeaderStyle}" Margin="0,0,0,16"/>
                    
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="المراقبة الفورية" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="فحص الملفات الجديدة والمعدلة تلقائياً" Style="{StaticResource BodyStyle}"/>
                        </StackPanel>
                        
                        <ToggleButton Grid.Column="1" 
                                      Style="{StaticResource ToggleSwitchStyle}"
                                      IsChecked="{Binding IsMonitoringEnabled, Mode=TwoWay}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- إعدادات التحديثات -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="⬆️" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="التحديثات" Style="{StaticResource SubHeaderStyle}"/>
                    </StackPanel>

                    <!-- الإصدار الحالي -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="الإصدار الحالي:" Foreground="{StaticResource TextSecondaryBrush}" Width="120"/>
                        <TextBlock Text="1.0.0" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                    </StackPanel>

                    <!-- حالة التحديث -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="حالة التحديث:" Foreground="{StaticResource TextSecondaryBrush}" Width="120"/>
                        <TextBlock Text="{Binding UpdateStatus}" 
                                   Foreground="{StaticResource TextPrimaryBrush}" 
                                   FontWeight="SemiBold"/>
                    </StackPanel>

                    <!-- شريط التقدم -->
                    <ProgressBar Value="{Binding UpdateProgress}" 
                                 Maximum="1"
                                 Height="6"
                                 Margin="0,0,0,16"
                                 Visibility="{Binding IsUpdating, Converter={StaticResource BoolToVisibility}}"/>

                    <!-- إشعار التحديث الجديد -->
                    <Border Background="#1A4CAF50" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,16"
                            Visibility="{Binding IsGitHubUpdateAvailable, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="🎉" FontSize="20" Margin="0,0,10,0"/>
                                <TextBlock Text="تحديث جديد متاح!" 
                                           Foreground="#4CAF50" 
                                           FontWeight="Bold"
                                           FontSize="16"/>
                            </StackPanel>
                            
                            <TextBlock Text="{Binding LatestVersion, StringFormat='الإصدار الجديد: {0}'}" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,4"/>
                            
                            <TextBlock Text="{Binding ReleaseNotes}" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       TextWrapping="Wrap"
                                       MaxHeight="100"
                                       Margin="0,0,0,12"/>
                            
                            <Button Content="تحميل وتحديث الآن"
                                    Command="{Binding DownloadUpdateCommand}"
                                    Background="#4CAF50"
                                    Foreground="White"
                                    Padding="20,10"
                                    HorizontalAlignment="Left"
                                    IsEnabled="{Binding IsUpdating, Converter={StaticResource InverseBoolToVisibility}}"/>
                        </StackPanel>
                    </Border>

                    <!-- التحديث التلقائي -->
                    <Grid Margin="0,8,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="التحديث التلقائي" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="تحديث قاعدة التوقيعات ونموذج الذكاء الاصطناعي تلقائياً" Style="{StaticResource BodyStyle}"/>
                        </StackPanel>

                        <ToggleButton Grid.Column="1"
                                      Style="{StaticResource ToggleSwitchStyle}"
                                      IsChecked="{Binding AutoUpdateEnabled, Mode=TwoWay}"/>
                    </Grid>

                    <!-- أزرار التحديث -->
                    <StackPanel Orientation="Horizontal">
                        <Button Content="التحقق من التحديثات"
                                Command="{Binding CheckUpdatesCommand}"
                                Style="{StaticResource ModernButtonStyle}"
                                Padding="16,8"
                                Margin="0,0,10,0"
                                IsEnabled="{Binding IsUpdating, Converter={StaticResource InverseBoolToVisibility}}"/>
                        
                        <Button Content="تحديث الآن"
                                Command="{Binding UpdateNowCommand}"
                                Style="{StaticResource ModernButtonStyle}"
                                Padding="16,8"
                                IsEnabled="{Binding IsUpdating, Converter={StaticResource InverseBoolToVisibility}}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- إعدادات الفحص -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="إعدادات الفحص" Style="{StaticResource SubHeaderStyle}" Margin="0,0,0,16"/>
                    
                    <TextBlock Text="أنواع الملفات المفحوصة:" 
                               Foreground="{StaticResource TextPrimaryBrush}" 
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    
                    <WrapPanel>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".exe" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".dll" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".sys" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".bat" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".ps1" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".vbs" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".msi" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                    </WrapPanel>
                </StackPanel>
            </Border>
            
            <!-- معلومات التطبيق -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="حول التطبيق" Style="{StaticResource SubHeaderStyle}" Margin="0,0,0,16"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="الإصدار:" Foreground="{StaticResource TextSecondaryBrush}" Width="100"/>
                        <TextBlock Text="1.0.0" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="المحرك:" Foreground="{StaticResource TextSecondaryBrush}" Width="100"/>
                        <TextBlock Text="ML.NET + Static Analysis" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="الإطار:" Foreground="{StaticResource TextSecondaryBrush}" Width="100"/>
                        <TextBlock Text=".NET 8.0" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    
                    <Border Background="{StaticResource PrimaryBrush}" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,16,0,0">
                        <StackPanel>
                            <TextBlock Text="🛡️ ShieldAI" 
                                       Foreground="White" 
                                       FontSize="18" 
                                       FontWeight="Bold"/>
                            <TextBlock Text="مكافح الفيروسات الذكي - مدعوم بالذكاء الاصطناعي" 
                                       Foreground="White" 
                                       Opacity="0.9"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
