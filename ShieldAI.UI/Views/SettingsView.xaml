<UserControl x:Class="ShieldAI.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="الإعدادات" Style="{StaticResource HeaderStyle}" Margin="0,0,0,24"/>
            
            <!-- إعدادات الحماية -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="الحماية في الوقت الحقيقي" Style="{StaticResource SubHeaderStyle}" Margin="0,0,0,16"/>
                    
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="المراقبة الفورية" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="فحص الملفات الجديدة والمعدلة تلقائياً" Style="{StaticResource BodyStyle}"/>
                        </StackPanel>
                        
                        <ToggleButton Grid.Column="1" 
                                      Style="{StaticResource ToggleSwitchStyle}"
                                      IsChecked="{Binding IsMonitoringEnabled, Mode=TwoWay}"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- إعدادات الفحص -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="إعدادات الفحص" Style="{StaticResource SubHeaderStyle}" Margin="0,0,0,16"/>
                    
                    <TextBlock Text="أنواع الملفات المفحوصة:" 
                               Foreground="{StaticResource TextPrimaryBrush}" 
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    
                    <WrapPanel>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".exe" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".dll" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".sys" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".bat" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".ps1" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".vbs" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                        <Border Background="{StaticResource BackgroundHoverBrush}" 
                                CornerRadius="16" Padding="12,6" Margin="0,0,8,8">
                            <TextBlock Text=".msi" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </Border>
                    </WrapPanel>
                </StackPanel>
            </Border>
            
            <!-- إعدادات VirusTotal -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="🌐" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="VirusTotal Integration" Style="{StaticResource SubHeaderStyle}"/>
                    </StackPanel>
                    
                    <TextBlock Text="اربط حسابك في VirusTotal للفحص عبر 70+ محرك antivirus" 
                               Style="{StaticResource BodyStyle}" Margin="0,0,0,16"/>
                    
                    <TextBlock Text="API Key:" 
                               Foreground="{StaticResource TextPrimaryBrush}" 
                               FontWeight="SemiBold" Margin="0,0,0,8"/>
                    
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox x:Name="ApiKeyTextBox"
                                 Text="{Binding VirusTotalApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Background="{StaticResource BackgroundHoverBrush}"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"
                                 Padding="12,10"
                                 FontFamily="Consolas"
                                 FontSize="13"/>
                        
                        <Button Grid.Column="1" 
                                Content="اختبار الاتصال"
                                Command="{Binding TestApiKeyCommand}"
                                Background="{StaticResource PrimaryBrush}"
                                Foreground="White"
                                Padding="16,10"
                                Margin="10,0,0,0"
                                Cursor="Hand"/>
                    </Grid>
                    
                    <StackPanel Orientation="Horizontal" 
                                Visibility="{Binding ApiKeyStatus, Converter={StaticResource StringToVisibility}}">
                        <TextBlock Text="{Binding ApiKeyStatusIcon}" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding ApiKeyStatus}" 
                                   Foreground="{Binding ApiKeyStatusColor}"/>
                    </StackPanel>
                    
                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="استخدام VirusTotal في AI Scan" 
                                       Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="فحص الملفات عبر VirusTotal أثناء التحليل العميق" 
                                       Style="{StaticResource BodyStyle}"/>
                        </StackPanel>
                        
                        <ToggleButton Grid.Column="1" 
                                      Style="{StaticResource ToggleSwitchStyle}"
                                      IsChecked="{Binding UseVirusTotalInAIScan, Mode=TwoWay}"/>
                    </Grid>
                    
                    <Border Background="#1A667eea" CornerRadius="8" Padding="12" Margin="0,16,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💡" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="احصل على API Key مجاني من virustotal.com" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- إعدادات AI Scan -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="🤖" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="AI Scan Settings" Style="{StaticResource SubHeaderStyle}"/>
                    </StackPanel>
                    
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="التحليل العميق" 
                                       Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="استخدام جميع محركات الكشف للتحليل الشامل" 
                                       Style="{StaticResource BodyStyle}"/>
                        </StackPanel>
                        
                        <ToggleButton Grid.Column="1" 
                                      Style="{StaticResource ToggleSwitchStyle}"
                                      IsChecked="{Binding EnableDeepAnalysis, Mode=TwoWay}"/>
                    </Grid>
                    
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="استخدام ML.NET" 
                                       Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="تصنيف الملفات باستخدام الذكاء الاصطناعي" 
                                       Style="{StaticResource BodyStyle}"/>
                        </StackPanel>
                        
                        <ToggleButton Grid.Column="1" 
                                      Style="{StaticResource ToggleSwitchStyle}"
                                      IsChecked="{Binding UseMLDetection, Mode=TwoWay}"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- معلومات التطبيق -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="حول التطبيق" Style="{StaticResource SubHeaderStyle}" Margin="0,0,0,16"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="الإصدار:" Foreground="{StaticResource TextSecondaryBrush}" Width="100"/>
                        <TextBlock Text="1.0.0" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="المحرك:" Foreground="{StaticResource TextSecondaryBrush}" Width="100"/>
                        <TextBlock Text="ML.NET + Static Analysis" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="الإطار:" Foreground="{StaticResource TextSecondaryBrush}" Width="100"/>
                        <TextBlock Text=".NET 8.0" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    
                    <Border Background="{StaticResource PrimaryBrush}" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,16,0,0">
                        <StackPanel>
                            <TextBlock Text="🛡️ ShieldAI" 
                                       Foreground="White" 
                                       FontSize="18" 
                                       FontWeight="Bold"/>
                            <TextBlock Text="مكافح الفيروسات الذكي - مدعوم بالذكاء الاصطناعي" 
                                       Foreground="White" 
                                       Opacity="0.9"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
