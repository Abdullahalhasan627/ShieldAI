<UserControl x:Class="ShieldAI.UI.Views.QuarantineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- العنوان -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="الحجر الصحي" Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="الملفات المعزولة للحماية من التهديدات" 
                       Style="{StaticResource BodyStyle}"
                       Margin="0,8,0,0"/>
        </StackPanel>
        
        <!-- قائمة الملفات المحجورة -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- شريط الملخص والإجراءات -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="عدد الملفات: " Style="{StaticResource BodyStyle}"/>
                        <TextBlock Text="{Binding QuarantinedFiles.Count}" Foreground="{StaticResource TextPrimaryBrush}" Margin="4,0,16,0"/>
                        <TextBlock Text="الحجم الإجمالي: " Style="{StaticResource BodyStyle}"/>
                        <TextBlock Text="{Binding QuarantineCount}" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="تحديث"
                                Command="{Binding RefreshQuarantineCommand}"
                                Style="{StaticResource ModernButtonStyle}"
                                Padding="12,6"
                                Margin="0,0,8,0"/>
                        <Button Content="حذف الكل"
                                Command="{Binding DeleteAllQuarantinedFilesCommand}"
                                Style="{StaticResource DangerButtonStyle}"
                                Padding="12,6"/>
                    </StackPanel>
                </Grid>

                <ListView Grid.Row="1" ItemsSource="{Binding QuarantinedFiles}"
                          Style="{StaticResource ModernListViewStyle}"
                          ItemContainerStyle="{StaticResource ModernListViewItemStyle}">
                    <ListView.View>
                        <GridView>
                            <!-- اسم الملف من QuarantineItemMetadata.OriginalName -->
                            <GridViewColumn Header="الملف" Width="200" 
                                            DisplayMemberBinding="{Binding OriginalName}"/>

                            
                            <!-- المسار الأصلي -->
                            <GridViewColumn Header="المسار الأصلي" Width="300" 
                                            DisplayMemberBinding="{Binding OriginalPath}"/>
                            
                            <!-- الحجم (بالبايت من FileSize) -->
                            <GridViewColumn Header="الحجم (بايت)" Width="120" 
                                            DisplayMemberBinding="{Binding FileSize}"/>
                            
                            <!-- تاريخ الحجر من QuarantinedAt -->
                            <GridViewColumn Header="تاريخ الحجر" Width="170">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding QuarantinedAt, StringFormat='{}{0:yyyy/MM/dd HH:mm}'}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            
                            <!-- أزرار الإجراءات: تستدعي أوامر MainViewModel -->
                            <GridViewColumn Header="الإجراءات" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="استعادة" 
                                                    Style="{StaticResource SuccessButtonStyle}"
                                                    Padding="12,6"
                                                    Margin="0,0,8,0"
                                                    Command="{Binding DataContext.RestoreFileCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding}"/>
                                            <Button Content="حذف" 
                                                    Style="{StaticResource DangerButtonStyle}"
                                                    Padding="12,6"
                                                    Command="{Binding DataContext.DeleteQuarantinedFileCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
                
                <!-- رسالة عند عدم وجود ملفات - تظهر فقط عندما لا توجد ملفات -->
                <StackPanel Grid.Row="1" VerticalAlignment="Center" 
                            HorizontalAlignment="Center">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding QuarantinedFiles.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="🔒" FontSize="64" HorizontalAlignment="Center"/>
                    <TextBlock Text="لا توجد ملفات في الحجر الصحي" 
                               Style="{StaticResource SubHeaderStyle}"
                               HorizontalAlignment="Center"
                               Margin="0,16,0,0"/>
                    <TextBlock Text="جميع الملفات المشبوهة سيتم نقلها هنا تلقائياً" 
                               Style="{StaticResource BodyStyle}"
                               HorizontalAlignment="Center"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
