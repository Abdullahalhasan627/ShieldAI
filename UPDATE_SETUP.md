# 🔄 نظام تحديث ShieldAI من GitHub

## كيف يعمل النظام؟

يتحقق التطبيق تلقائياً من GitHub Releases للبحث عن إصدارات جديدة. عندما يجد تحديثاً:
1. يظهر إشعار في واجهة المستخدم
2. يمكن للمستخدم تحميل التحديث بنقرة واحدة
3. يتم إغلاق التطبيق وتثبيت التحديث تلقائياً
4. يُعاد تشغيل التطبيق كمسؤول

---

## ⚙️ خطوات الإعداد

### 1. تغيير معلومات GitHub

افتح الملف `ShieldAI.UI/ViewModels/SettingsViewModel.cs` وابحث عن:

```csharp
_gitHubUpdateService = new GitHubUpdateService("YOUR_GITHUB_USERNAME", "YOUR_REPO_NAME");
```

استبدلها بمعلوماتك:

```csharp
_gitHubUpdateService = new GitHubUpdateService("alhas", "ShieldAI");
```

---

### 2. رفع التحديث على GitHub Releases

#### أ. بناء المشروع كـ Release

```powershell
cd c:\Users\alhas\OneDrive\Desktop\innovative-idea
dotnet publish ShieldAI.UI\ShieldAI.UI.csproj -c Release -r win-x64 --self-contained true
```

#### ب. إنشاء ملف ZIP

```powershell
$source = "ShieldAI.UI\bin\Release\net8.0-windows\win-x64\publish"
$dest = "ShieldAI-v1.1.0.zip"
Compress-Archive -Path "$source\*" -DestinationPath $dest
```

#### ج. رفع الإصدار على GitHub

1. اذهب إلى https://github.com/YOUR_USERNAME/YOUR_REPO/releases
2. اضغط "Draft a new release"
3. اختر "Choose a tag" واكتب: `v1.1.0`
4. في "Release title": `ShieldAI v1.1.0`
5. في "Describe this release": اكتب التغييرات
6. اسحب ملف `ShieldAI-v1.1.0.zip` إلى قسم "Attach binaries"
7. اضغط "Publish release"

---

### 3. تحديث رقم الإصدار

افتح ملف `ShieldAI.UI/ShieldAI.UI.csproj` وأضف:

```xml
<PropertyGroup>
  <Version>1.1.0</Version>
  <AssemblyVersion>1.1.0</AssemblyVersion>
  <FileVersion>1.1.0</FileVersion>
</PropertyGroup>
```

---

### 4. هيكل الإصدار على GitHub

```
📦 github.com/YOUR_USERNAME/YOUR_REPO/releases
├── 📁 v1.1.0 (latest)
│   ├── 📎 ShieldAI-v1.1.0.zip  ← ملف التحديث
│   └── 📝 Release Notes
├── 📁 v1.0.0
│   └── 📎 ShieldAI-v1.0.0.zip
```

---

## 📋 ملاحظات مهمة

### 🔒 الصلاحيات
التطبيق يتطلب صلاحيات مسؤول للتشغيل والتحديث. سكريبت PowerShell يعيد التشغيل كمسؤول تلقائياً.

### 🔄 آلية التحديث

1. **التحقق**: يتصل بـ `api.github.com/repos/USER/REPO/releases/latest`
2. **المقارنة**: يقارن `tag_name` (مثلاً `v1.1.0`) مع الإصدار الحالي
3. **التحميل**: يحمل الملف `.zip` من GitHub
4. **النسخ الاحتياطي**: ينشئ نسخة احتياطية في `backup/`
5. **التثبيت**: يشغل PowerShell لاستبدال الملفات
6. **إعادة التشغيل**: يعيد تشغيل التطبيق كمسؤول

### ⚠️ ملاحظات أمنية

- يتم التحقق من تحديثات فقط من GitHub API
- لا يتم تنفيذ `git pull` مباشرة (لأن المستخدم النهائي لن يكون لديه Git)
- يتم استخدام PowerShell لضمان صلاحيات الكتابة

---

## 🧪 اختبار التحديث

### 1. اختبار محلي

```powershell
# 1. عدل رقم الإصدار في .csproj إلى 1.0.0
# 2. ابني المشروع
# 3. شغل التطبيق
# 4. اضغط "التحقق من التحديثات"
```

### 2. اختبار حقيقي

```powershell
# 1. ارفع إصدار 1.1.0 على GitHub
# 2. غير رقم الإصدار المحلي إلى 1.0.0
# 3. ابني وشغل
# 4. يجب أن يظهر "تحديث جديد متاح"
# 5. اضغط "تحميل وتحديث الآن"
```

---

## 🐛 استكشاف الأخطاء

### مشكلة: "تعذر التحقق من التحديثات"

**الحل**: تأكد من:
- اتصال الإنترنت
- صحة اسم المستخدم والمستودع
- أن المستودع عام (Public)

### مشكلة: "فشل التحديث"

**الحل**: تأكد من:
- تشغيل التطبيق كمسؤول
- وجود مساحة كافية على القرص
- أن ملف ZIP يحتوي على جميع الملفات المطلوبة

---

## 📁 الملفات الجديدة/المعدلة

| الملف | الوصف |
|-------|-------|
| `GitHubUpdateService.cs` | خدمة التحديث من GitHub |
| `SettingsViewModel.cs` | تعديل لدعم التحديث |
| `SettingsView.xaml` | واجهة مستخدم جديدة للتحديث |
